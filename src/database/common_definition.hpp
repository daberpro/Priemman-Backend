#pragma once
#include <string>

namespace priemman::common {

    struct PublicInfo{
        std::string id;
        std::string first_name;
        std::string last_name;
        std::string avatar_url;
    };

}
